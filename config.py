SITE_URL = ''
SITE_NAME = '' # Used for the logo and the page title

# INPUT
INPUT_DIRECTORY = '' # Name of the input directory
TEMPLATE_FILE = 'template.html'
STATIC_DIRECTORY = 'static'

# OUTPUT
OUTPUT_DIRECTORY = 'output'
MEDIA_DIRECTORY = 'media'  # Where the non-markdown files will be copied to

# OPTIONS
# If STRICT is True, generation fails if a local link cannot be resolved.
# If STRICT is False, unresolved links/embeded files are removed.
STRICT = False
